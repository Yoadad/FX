@model IEnumerable<XF.Models.ProductItemViewMOdel>

@{
    ViewBag.Title = "Products";
}

<input type="hidden" id="hfPageSize" value="@ViewBag.PageSize" />

<h2>Products</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Product.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.SellPrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.PurchasePrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Max)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Min)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stock)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.SellPrice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.PurchasePrice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Max)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Min)
        </td>
        <td>
            @if (User.IsInRole("Admin") || User.IsInRole("Super"))
            {
                @Html.ActionLink(item.Stock.ToString(), string.Format("Stock/{0}", item.Product.Id), "Products")
            }
            else
            {
                @Html.DisplayFor(modelItem => item.Stock)
            }

            


        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Product.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Product.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Product.Id })
        </td>
    </tr>
}
</table>*@

<div id="grid"></div>
@section scripts
    {
@Scripts.Render("~/Scripts/ProductStock/ProductStock.js")
    
    }
